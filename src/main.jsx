import React from "react";
import ReactDOM from "react-dom/client";
import SLKManagerPrototype from "./SLKManager.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SLKManagerPrototype />
  </React.StrictMode>
);
